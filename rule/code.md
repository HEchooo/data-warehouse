# 代码规则

## DDL

- 如果要修改表中字段，不要修改 `ddl` 目录中现有的 `CREATE TABLE` 语句。
- 应该使用 `ALTER TABLE` 语句在原始定义下方修改字段。

## 路径

- 不要修改代码库中的任何文件路径。
- 这些路径可能直接引用生产环境。

## 清理

- 完成任务后，删除编译生成的文件夹，例如 `__pycache__`。

## 可读性

- 保持代码高可读性。
- 修改时优先做最小必要变更，不要顺手制造无关重构。
